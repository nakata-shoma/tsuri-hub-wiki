# CLAUDE.md

## プロジェクト概要
TSURI HUB(開発中の釣り関連Webアプリ)の新機能として追加予定の「釣りwikiページ」向けの情報収集プロジェクト。
本リポジトリはTSURI HUB本体のリポジトリとは完全に独立しており、コード実装ではなく調査資料・下書きの蓄積を目的とする。

- 本番サイト: https://tsuri-hub.com/
- 本体リポジトリ(開発中コード): `C:\Users\nakat\Desktop\main\100_WebApp\turi`(本プロジェクトとは別リポジトリ)

## 目的
wikiページ実装時の元原稿となるリサーチメモ・下書きを作成すること。

## 情報の範囲
- 魚種図鑑(生態・特徴・旬・生息域など)
- 釣り方(釣法・誘い方・釣り方ごとのコツ)
- タックル(ロッド/リール/ラインなどの道具情報)
- ノット・結び方(ラインとリーダー、ラインと針などの結束方法)
- 釣り場情報(エリア、ポイント、遊漁券・禁漁区などの規則)
- 用語集・初心者向け解説
- 安全・危険生物(危険な魚・生き物、熱中症・低体温症・落水などの安全情報)
- 初心者ガイド(始め方、最初に揃える道具一式、潮汐・天気の読み方などの実践ガイド、他カテゴリへの導線)
- 季節・シーズナルカレンダー(いつ・どこで・何が釣れやすいかの目安。魚種横断で1ファイルに一本化し、詳細はspecies/locationにリンクして重複記述を避ける)
- 魚の持ち帰り・下処理・料理(締め方・保存方法・簡単なレシピ)
- マナー・ルール(ゴミの持ち帰り、駐車、他の釣り人との距離など、特定水域の法令ではない全国共通の一般マナー)

## 成果物の形式
- テーマごとのリサーチメモ・下書き(Markdown)
- 1テーマ = 1ファイルを基本とする(密接に関連する複数語をまとめる例外は[INDEX.md](INDEX.md)運用メモ参照)
- ファイル名は `カテゴリ_トピック名.md`(例: `species_kuroudai.md`, `technique_ajing.md`, `tackle_reel.md`, `knot_clinchknot.md`, `location_xxx.md`, `glossary_xxx.md`, `safety_xxx.md`, `beginner_xxx.md`, `seasonal_xxx.md`, `cooking_xxx.md`, `manner_xxx.md`)
- 各ファイル冒頭にステータス(調査中 / 下書き / 要出典確認 / 完了)を記載する(凡例は[INDEX.md](INDEX.md)参照)

## 情報収集時の注意事項
- 一次情報(公的機関、専門書、メーカー公式サイト等)を優先し、出典(URL・書籍名・確認日)を各メモに明記する。
- 遊漁券・禁漁期間・禁漁区・サイズ制限などの法令・規則は都道府県・水域ごとに異なるため断定的に書かず、必ず出典と確認日を残す。誤情報がユーザーの法令違反につながりうるため特に慎重に扱う。
- 生物の呼称は標準和名と地方名を区別して記載する。
- マアジ・マイワシ・マサバのように「マ(真)」を冠した標準和名が特定の1種のみを指す魚種は、species記事のタイトル・トピック名は総称(アジ・イワシ・サバ等)を用いる。本文では総称の代表種として当該種(マアジ等)を扱いつつ、近縁種(ムロアジ、カタクチイワシ、ゴマサバなど釣り・食用として紛らわしい種)の呼称・生態・見分け方の違いにも触れ、総称としての記事として構成する。ファイル名も総称に合わせる(例: `species_aji.md`、`species_iwashi.md`)。
- 未確認・推測を含む内容は本文と明確に区別し、「要確認」等のマーカーを付ける。
- ノット(結び方)は手順を文章だけで正確に伝えるのが難しいため、参考にした図解・動画のURLを出典に明記し、本文では手順の要点と用途・強度の目安を中心にまとめる。
- 安全・危険生物の情報(毒・応急処置など)は、遊漁券・禁漁区と同様に誤りが実害につながりうるため、出典と確認日を必須とし断定的な記載を避ける。応急処置は一次的な目安にとどめ、必要に応じて医療機関の受診を促す旨を添える。
- 季節・シーズナルカレンダーは、魚種・エリアの詳細情報をspecies_*.md/location_*.mdへのリンクで参照する構成とし、内容の重複記述を避ける。

## 執筆ルール
- 本文は日本語で記述する(TSURI HUBのユーザー向けコンテンツのため)。
- 専門用語には初心者にもわかる簡単な補足を添える(用語集との連携を意識する)。
- 誇張・断定的な安全性主張(例: 「絶対に釣れる」「危険はない」)は避ける。
- 文体は常体(だ・である調)で統一する。敬体(です・ます調)は一切使用しない(例: 「〜のことです」→「〜のことである」、「〜が使われます」→「〜が使われる」)。frontmatterのコメントやINDEX.md等の管理用文書は対象外。

### 可読性のための基本ルール

- 句点(。)で改行する。1文1行を基本とし、Markdownソース上での可読性(編集・差分確認のしやすさ)を優先する。
- 一文一義を意識する。読点が3つ以上続くような長文は、文を分割する。
- 列挙的な内容は文章で書き連ねず、箇条書きを積極的に使う。
- 接続詞・形式名詞・補助動詞はひらがなで統一して開く(例: 「事」→「こと」、「出来る」→「できる」、「〜して下さい」→「〜してください」、「又は」→「または」)。

## TSURI HUB本体への統合方針(確定)
本体(`100_WebApp/turi`)側に`apps.wiki`(`WikiArticleModel`)が実装済み。本リポジトリはその正(source of truth)として機能する。

- **リポジトリ**: [https://github.com/nakata-shoma/tsuri-hub-wiki](https://github.com/nakata-shoma/tsuri-hub-wiki)(public)。`main`ブランチにマージされた内容のみが本体へ取り込み対象になる。
- **取り込み方式**: 本体側の`python manage.py import_wiki --github-repo https://github.com/nakata-shoma/tsuri-hub-wiki.git`(開発環境ではローカルパスを`--source <path>`で指定することも可)が、frontmatter(status/category/last_updated)と本文を解析してDBへupsertする。本体のDBではなく、常に本リポジトリのMarkdownが正。
- **本番反映**: 本番サーバーには本リポジトリの永続的なチェックアウトを置かない。GitHubからオンデマンドで一時取得し、インポート後は破棄する(専用サーバーは新設しない)。
- **公開制御**: `status`が`完了`の記事のみ、インポート時に自動的に公開(`is_published=True`)される。`完了`のままである限り、本体の管理画面で手動非公開にしても次回インポートで公開に戻る点に注意(緊急停止したい場合は本リポジトリ側の`status`も`完了`から外すこと)。`完了`以外のステータスでは`is_published`を自動変更しないため、`調査中`/`下書き`/`要出典確認`の間は管理画面での手動公開・非公開操作がそのまま保持される。
- **本文中の内部リンク**: `[glossary_xxx.md](glossary_xxx.md)`のような相対Markdownリンクは、インポート時に本体サイトのURL(`/wiki/<category>/<slug>/`)へ自動変換される。リンク文字列がファイル名そのままの場合はリンク先記事のタイトルに置き換わり、本文中で意図的に付けた文言(例: `[居着き](glossary_itsuki.md)`)はそのまま維持される。

## 画像の扱い(将来対応・現時点では未実装)
画像は本リポジトリ(git)には含めない方針とする。理由: gitはバイナリの差分管理が苦手で、画像を追加するとリポジトリが編集のたびに際限なく肥大化するため。

- 画像を追加する際は、本Markdownリポジトリとは別に画像専用のステージング用フォルダ(gitで管理しない)を用意し、`category/slug`に対応するサブフォルダ構成で配置する想定(例: `species/species_kuroudai/01.jpg`)。
- 本体側で`import_wiki`と同様の一括インポートコマンド(例: `import_wiki_images`)を用意し、該当記事に画像を紐付けてDjangoのメディアストレージ(将来的にはS3等への切替も可能)へ保存する設計とする。1記事ずつ管理画面で手動アップロードする運用は想定しない。
- 本文中に画像を挿入する際の記法(パス指定方法など)は、画像機能の実装着手時に別途ルール化する。

## 今後の方針(未決定事項)
- 画像を本文に挿入する際のMarkdown記法・命名ルールは、画像機能の実装着手時に定める。
- 本リポジトリでのコード実装は行わない。コードが追加される場合は、グローバル指示のコーディング規約に従う。
